## Tests use Mocha + Chai

```javascript
describe('Cypress presentation', () => {
  beforeEach(() => {
    ...
  });

  context('benefits', () => {
      it('should educate listeners on Cypress', () => {
        ...
      });
  });

  context('attributes', () => {
      it('should last about an hour', () => {
        ...
      });
  });
});
```
